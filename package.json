{
  "name": "easyfetch-client",
  "displayName": "EasyFetch Client",
  "publisher": "Mhocine",
  "description": "EasyFetch Client is a vs code extension that will help you test your apis directly from vs code ",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "easyfetch-client.EasyFetch",
        "title": "Easy Fetch Client"
      }
    ]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.9",
    "@types/node": "20.x",
    "@types/vscode": "^1.95.0",
    "@vscode/test-cli": "^0.0.10",
    "@vscode/test-electron": "^2.4.1",
    "eslint": "^9.13.0"
  },
  "dependencies": {
    "@hocinemk/easyfetch": "^1.0.4"
  }
}
